/// A fixed number for the `Border Radius`
const double fixedBorderRadius = 10;

/// A fixed number for `Circular Border Radius`
const double fixedBorderRadiusfullyCircular = 100;

/// A fixed number for the `Blur Shadow`
const double fixedBlurForShadow = 5.0;

/// A fixed number for the `Padding`
const double fixedPadding = 10;

/// A fixed main padding number for the App,
/// This hold the main 'Horizontal' padding that used in the App
const double fixedMainPadding = 25;

/// Cursor width
const double cursorWidth = 2.5;
